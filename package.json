{
  "name": "spenn",
  "module": "server.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts",
    "bundle": "bun build ./index.ts --outfile=./build/bundle.js"
  }
}
